import 'package:flutter/material.dart';

class FeedProfileWidget extends StatelessWidget {
  const FeedProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
